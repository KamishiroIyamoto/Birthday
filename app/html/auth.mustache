<!doctype html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <meta name="viewport"
          content="width=device-width, user-scalable=no, initial-scale=1.0, maximum-scale=1.0, minimum-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <title>Авторизация</title>
</head>
<body>
    <h1>Авторизация</h1>

    <form method="post" action="/auth">
        <label>
            Телефон
            <input type="tel" name="phone" placeholder="78005553535" pattern="[0-9]{11}" value="{{phone}}" required>
        </label>
        <br><br>

        {{#button}}
        <label>
            Код из сообщения
            <input name="code" placeholder="12345" pattern="[0-9]{5}" value="{{code}}" required>
        </label>
        <br><br>
        {{/button}}

        <button type="submit">{{button}}{{^button}}Отправить код{{/button}}</button>
    </form>
</body>
</html>